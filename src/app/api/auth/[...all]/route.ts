// import { toNextJsHandler } from "better-auth/next-js";
// import { auth } from "~/server/better-auth";

export const GET = () =>
	new Response("Better Auth is disabled", { status: 200 });
export const POST = () =>
	new Response("Better Auth is disabled", { status: 200 });
